import React from 'react';
import Counter from './Counter'; // Assuming the component is in a file named Counter.js

const App = () => {
  return (
    <div>
      <Counter />
    </div>
  );
};

export default App;
